{
  "scripts": {
    "tdd:step:green": "git gamble --green -- pnpx vitest run",
    "tdd:step:red": "git gamble --red -- pnpx vitest run",
    "tdd:step:refactor": "git gamble --refactor -- pnpx vitest run",
    "test": "vitest",
    "test:ui": "vitest --ui"
  },
  "devDependencies": {
    "@vitest/ui": "^3.1.1",
    "vitest": "^3.1.1"
  }
}
