{
  "scripts": {
    "tdd:step:green": "git gamble --green -- pnpm run test",
    "tdd:step:red": "git gamble --red -- pnpm run test",
    "tdd:step:refactor": "git gamble --refactor -- pnpm run test",
    "test": "vitest",
    "test:ui": "vitest --ui"
  },
  "devDependencies": {
    "@vitest/ui": "^3.1.1",
    "vitest": "^3.1.1"
  }
}
